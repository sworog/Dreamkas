package project.lighthouse.autotests.elements.preLoader;

import org.openqa.selenium.WebDriver;

public class ProductEditionPreLoader extends AutocompletePreLoader {

    public ProductEditionPreLoader(WebDriver driver) {
        super(driver);
    }
}

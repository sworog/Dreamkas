define(function(require, exports, module) {
    //requirements
    var Form = require('blocks/form/form');

    require('lodash');

    return Form.extend({
        el: '.form_barcodes',
        render: function(){}
    });
});
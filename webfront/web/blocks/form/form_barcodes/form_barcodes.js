define(function(require, exports, module) {
    //requirements
    var Form = require('blocks/form/form'),
        Page = require('kit/page/page');

    require('lodash');

    return Form.extend({
        el: '.form_barcodes',
        render: function(){}
    });
});
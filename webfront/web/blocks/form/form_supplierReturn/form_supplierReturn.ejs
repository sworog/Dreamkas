<form id="form_supplierReturn" class="form form_supplierReturn">
    <div class="row">
        <div class="col-md-4">
            <div class="form-group">

                <label class="control-label">Дата *</label>

                <b block="inputDate"
                   data-value="<%- model.get('date') ? helpers.formatDate(model.get('date')) : helpers.formatDate(new Date()) %>"></b>

            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">

                <label class="control-label">Магазин *</label>

                <div class="input-icon right">
                    <select class="form-control" name="store">
                        <option value="" selected disabled>Выберите магазин</option>
                        <% collections.stores.forEach(function(storeModel){ %>
                        <option <%- model.get('store.id') === storeModel.id ? 'selected' : '' %>
                                value="<%- storeModel.id %>"><%- storeModel.get('name') %>
                        </option>
                        <% }) %>
                    </select>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">

                <label class="control-label">Поставщик</label>

                <div class="row">
                    <div class="col-md-12">
                        <%= partials.select_suppliers({
                            collections: {
                                suppliers: collections.suppliers
                            },
                            selected: model.get('supplier.id')
                        }) %>
                    </div>
                </div>

            </div>
        </div>
    </div>
</form>
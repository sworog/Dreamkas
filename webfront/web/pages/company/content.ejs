<%
/**
 *
 * @collections.organizations
 */
%>

<div class="content">
    <h1>Компания</h1>

    <% if (collections.organizations.length) { %>

    <h2>Организации</h2>

    <% collections.organizations.forEach(function(organizationModel){ %>

    <div style="padding-bottom: 20px;">
        <a name="organizationLink"
           href="/company/organizations/<%- organizationModel.id %>"><%- organizationModel.get('name') %></a>
    </div>

    <% }) %>

    <% } else { %>
        <p>В вашей компании пока нет организаций</p>
    <% } %>
</div>
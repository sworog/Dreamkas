define(function(require, exports, module) {
    //requirements
    var getText = require('kit/utils/getText');

    return {
        kg: function(format, value){
            switch (format) {
                case ''
            }
        },
        unit: function(format, value){

        },
        liter: function(format, value){

        }
    }
});